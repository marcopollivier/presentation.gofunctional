# Lab __NAME__

> **O ponto:** [uma frase — o que este lab prova no palco.]

**Go:** `__GOVERSION__` — [por que esta versão. Se for a mesma da raiz, "a
corrente"; se for anterior, diga o que ela demonstra que a atual já não
demonstra.]

**Bloco da palestra:** [n]

## Como rodar

```bash
make run     # executa
make test    # testes
make all     # vet + test
```

Ou, da raiz do repo:

```bash
make lab LAB=__NAME__
```

## Notas

[O que observar na saída. Se houver um "antes/depois", diga qual comando mostra
cada lado.]
